const path = require('path');

module.exports = {
  // The outputFileTracingRoot option is for monorepo setups. If your project is not a monorepo
  // or if this setting is causing issues, you can try commenting it out.
  // outputFileTracingRoot: path.join(__dirname, '..'),
};